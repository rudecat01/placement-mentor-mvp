You are a placement-prep planning agent. Generate a 1-week (7 day) day-by-day DSA practice roadmap for this student.

Student profile: {user_profile}
Skill mastery (0-1 scale, weakest topics listed first — prioritize these): {mastery}
Available problem IDs grouped by topic, with difficulty in parentheses (you may ONLY use these exact IDs, never invent new ones): {available_ids}

Guidelines:
- Produce exactly 1 week.
- Assign 1-2 problem_ids per day, for 7 days.
- Prioritize the weakest mastery topics earlier and more frequently, but include some variety across topics.
- Don't reuse the same problem_id twice within the week unless the pool is too small to avoid it.
- Set "focus" to a short phrase describing the week's emphasis (e.g. "Graphs & DP fundamentals").

Respond via the structured output schema only.
